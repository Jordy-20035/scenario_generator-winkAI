# 🎬 Генератор препродакшн-таблиц для киносценариев

Автоматический сервис для создания препродакшн-таблиц из текстовых сценариев на русском языке.

## 📋 Описание

Сервис автоматически анализирует киносценарии в формате PDF или DOCX и извлекает все производственные элементы:
- Номера серий и сцен с сегментацией
- Локации (объект, подобъект, синопсис)
- Время суток (режим: день, ночь, утро, вечер)
- Интерьер/натура (Инт/нат)
- Персонажи (основные и второстепенные)
- Массовка
- Реквизит, игровой транспорт, животные
- Грим и костюмы
- Трюки и каскадеры
- Время года
- И другие производственные элементы

Результат представляется в виде структурированной таблицы с возможностью редактирования и экспорта в CSV или XLSX.

## 🚀 Быстрый старт

### Предварительные требования

- Docker и Docker Compose (рекомендуется)
- Или Python 3.11+ с установленными зависимостями

### Запуск с Docker (рекомендуется)

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd scenario_generator-winkAI-1
```

2. Запустите с помощью Docker Compose:
```bash
docker-compose up --build
```

3. Откройте в браузере:
   - Streamlit интерфейс: http://localhost:8501
   - FastAPI документация: http://localhost:8000/docs

### Запуск без Docker

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Загрузите необходимые модели (после установки зависимостей!):
```bash
# Windows
install_models.bat

# Linux/Mac
chmod +x install_models.sh && ./install_models.sh

# Или вручную:
python -m spacy download ru_core_news_sm
python -c "import nltk; nltk.download('punkt'); nltk.download('stopwords')"
```

3. Запустите FastAPI сервер (в одном терминале, из корневой директории проекта):
```bash
uvicorn backend.main:app --host 0.0.0.0 --port 8000
```

4. Запустите Streamlit приложение (в другом терминале, из корневой директории проекта):
```bash
streamlit run frontend/app.py --server.port 8501
```

## 📁 Структура проекта

```
scenario_generator-winkAI-1/
├── backend/                 # FastAPI backend
│   ├── main.py             # API endpoints
│   ├── document_parser.py  # Парсер PDF/DOCX
│   ├── scene_segmenter.py  # Сегментация сцен
│   ├── element_extractor.py # Извлечение элементов
│   └── table_generator.py  # Генерация таблиц
├── frontend/                # Streamlit frontend
│   └── app.py              # Главное приложение
├── requirements.txt         # Зависимости Python
├── Dockerfile              # Docker образ
├── docker-compose.yml      # Docker Compose конфигурация
└── README.md              # Документация
```

## 🎯 Использование

1. **Загрузка сценария**: Откройте вкладку "Загрузка и обработка" и загрузите файл сценария (PDF или DOCX).

2. **Выбор пресета**: В боковой панели выберите один из пресетов:
   - **Базовый**: Основные столбцы (Серия, Сцена, Режим, Инт/нат, Объект, Подобъект, Синопсис, Персонажи, Реквизит)
   - **Расширенный**: Добавляет Время года, Массовку, Грим, Костюм, Игровой транспорт, Трюк, Животные
   - **Полный**: Все доступные столбцы (тот же набор, что и расширенный)
   - **Кастомный**: Выберите свои столбцы из полного списка доступных

3. **Обработка**: Нажмите кнопку "Обработать сценарий". Обработка может занять до 5 минут.

4. **Просмотр и редактирование**: На вкладке "Результаты" просмотрите таблицу, при необходимости отредактируйте данные прямо в интерфейсе.

5. **Экспорт**: Скачайте результат в формате CSV или XLSX.

## 🔧 Технические детали

### Поддерживаемые форматы
- PDF (.pdf)
- DOCX (.docx)

### Поддерживаемые кодировки
- UTF-8
- UTF-16
- CP1251 (Windows-1251)
- KOI8-R
- ISO-8859-5
- MacRoman
- ASCII

### Ограничения
- Максимальный размер файла: ~120 страниц (примерно 6 МБ)
- Время обработки: до 5 минут для среднего сценария
- Все модели работают локально, без внешних API

### Используемые технологии
- **Backend**: FastAPI, spaCy, Natasha, PyPDF2, python-docx
- **Frontend**: Streamlit
- **NLP**: spaCy (ru_core_news_sm), Natasha, razdel
- **Экспорт**: pandas, openpyxl

## 📊 Примеры пресетов

### Базовый пресет
Содержит основные столбцы для базового препродакшн-планирования:
- **Серия** - номер серии
- **Сцена** - номер сцены
- **Режим** - время суток (день, ночь, утро, вечер)
- **Инт / нат** - интерьер или натура
- **Объект** - основная локация
- **Подобъект** - детализация локации
- **Синопсис** - краткое описание сцены
- **Персонажи** - список персонажей
- **Реквизит** - необходимый реквизит

### Расширенный пресет
Включает все столбцы базового пресета, плюс:
- **Время года** - сезон съемки
- **Массовка** - информация о массовке
- **Грим** - требования к гриму
- **Костюм** - требования к костюмам
- **Игровой транспорт** - транспортные средства
- **Трюк** - трюки и каскадеры
- **Животные** - животные в сцене

### Полный пресет
Содержит все доступные столбцы (тот же набор, что и расширенный пресет):
- Все столбцы из расширенного пресета
- Полный набор данных для комплексного препродакшн-планирования

## 🐛 Решение проблем

### Ошибка "ru_core_news_sm not found"
```bash
python -m spacy download ru_core_news_sm
```

### Ошибка подключения к API
Убедитесь, что FastAPI сервер запущен на порту 8000. Проверьте переменную окружения `API_URL` или используйте `http://localhost:8000`.

### Проблемы с кодировкой
Сервис автоматически определяет кодировку. Если возникают проблемы, проверьте, что файл сохранен в одной из поддерживаемых кодировок.

## 📝 Лицензия

Проект разработан для хакатона winkAI challenge.

## 👥 Автор

Manye Jordana - DiverCity
